// using context api so have to import context api 
// can do both context and state in one single file

import { createContext } from "react";
const NoteContext = createContext()

export default NoteContext